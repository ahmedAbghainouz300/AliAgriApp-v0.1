import { RightCurrencyPipe } from './right-currency.pipe';

describe('RightCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new RightCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
